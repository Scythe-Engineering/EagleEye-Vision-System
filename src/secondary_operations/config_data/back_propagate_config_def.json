{
    "class_name": "BackPropagate",
    "description": "Back propagates input data to a specified operation earlier in the pipeline by calling its back_propagate_input method, then passes the input through",
    "category": "proc",
    "input_nodes": [
        {
            "name": "data",
            "has_default": false
        }
    ],
    "output_nodes": ["data"],
    "parameters": {
        "action_name": {
            "type": "str",
            "description": "Name of the action to back propagate to (in snake_case)",
            "default": "",
            "required": true,
            "restart_for_change": false
        },
        "target_pipeline_name": {
            "type": "str",
            "description": "Optional name of the pipeline that contains the target operation",
            "default": "",
            "required": false,
            "restart_for_change": false
        }
    }
}
