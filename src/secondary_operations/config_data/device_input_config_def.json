{
    "class_name": "DeviceInput",
    "description": "Provides the initial device input for the pipeline",
    "category": "prep",
    "folder": "Input",
    "is_data_source": true,
    "input_nodes": [],
    "output_nodes": [
        "frame"
    ],
    "parameters": {
        "bus_id": {
            "type": "str",
            "description": "USB bus identifier for the camera (e.g., '1' for USB port 1)",
            "default": "",
            "required": true,
            "restart_for_change": true
        },
        "frame_rotation": {
            "type": "int",
            "description": "Rotation angle in degrees to apply to frames. Must be one of: 0, 90, 180, 270.",
            "default": 0,
            "required": false,
            "restart_for_change": false
        }
    }
}
