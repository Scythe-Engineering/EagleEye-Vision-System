{
    "class_name": "FusedCameraLocalizationDefinition",
    "description": "Combines multiple localization methods (PnP, YTD) to provide accurate and robust camera pose estimation using AprilTag detections",
    "category": "loc",
    "parameters": {
        "camera_parameters_path": {
            "type": "str",
            "description": "Path to the camera parameters file",
            "default": "{project_root}/config/camera_parameters.json",
            "required": true,
            "restart_for_change": true
        },
        "apriltag_map_path": {
            "type": "str",
            "description": "Path to the apriltag map file",
            "default": "{project_root}/config/apriltag_map.fmap",
            "required": true,
            "restart_for_change": true
        }
    }
}
