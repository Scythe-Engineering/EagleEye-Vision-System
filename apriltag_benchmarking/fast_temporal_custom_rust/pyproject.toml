[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "apriltag-fast-temporal"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy"]

[tool.maturin]
module-name = "apriltag_fast_temporal"
