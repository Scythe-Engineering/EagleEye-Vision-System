[pytest]
addopts = -ra
markers =
    hardware_skip: operation requires hardware or device access
    rust_optional: optional Rust extensions missing
    yolo_excluded: YOLO operations excluded from tests
filterwarnings =
    error
